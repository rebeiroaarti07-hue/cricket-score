# No live provider is enabled until its terms and redistribution rights are reviewed.
CRICKET_SCORE_PROVIDER=fixture
# Set DATABASE_URL to enable the PostgreSQL repository and run `npm run migrate` first.
# DATABASE_URL=postgres://cricket:cricket@localhost:5432/cricket_score